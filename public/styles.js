import { createStyles } from '@mantine/core'
const useStyles = createStyles((theme) => ({
  whiteFont: {
    color: 'white',
  },
  noSelect: {
    color: 'white',
    userSelect: 'none',
  },
}))
export default useStyles
